<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/dialog_category_delete_message"
        android:text="@string/category_deleting_message"
        android:textColor="#000000"
        android:textSize="9pt"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <CheckBox
        android:id="@+id/dialog_category_delete_checkbox"
        android:text="@string/category_deleting_checkbox_message"
        app:layout_constraintTop_toBottomOf="@id/dialog_category_delete_message"
        app:layout_constraintEnd_toEndOf="@id/dialog_category_delete_message"
        android:layout_marginTop="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>